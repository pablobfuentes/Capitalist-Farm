# 8.5 — Supply Chain Map Visualization

**Purpose:** Interactive Supply Chain View overlaid on the isometric farm map — curved dotted trade routes, ownership colors, direction animation, and path cycling for branches.

**Spec:** [`docs/SUpply_Chain_visualization.txt`](docs/SUpply_Chain_visualization.txt)

**Related:**
- Existing abstract diagram → `godot/ui/screens/supply_chain_diagram.gd` / `supply_chain_view.tscn` (keep; this feature is map-overlaid)
- Connection catalog → `godot/data/connections/supply_connection.gd` + `Content.connections`
- Active links / ownership math → `godot/core/systems/synergy_system.gd`
- Parcel ↔ business mapping → `godot/core/systems/parcel_ownership_system.gd`
- Map chrome → `godot/scenes/farm_map/iso_farm_map.*`

**Status values:** `[ ]` todo · `[~]` in progress · `[x]` done · `[-]` cut / deferred

---

## Decisions

| # | Question | Decision |
|---|----------|----------|
| Q1 | Blue “owned” connection | **Player owns both endpoint businesses** (supplier + customer). |
| Q2 | Clicks while mode on | **1st click** → select / show chain · **2nd click** (same building) → open parcel panel. |
| Q3 | Visible scope | **Focused district only** (not overview-wide). |
| Q4 | NPC / opportunity parcels | **Include** as nodes; their links render **white** until both ends are player-owned. |
| Q5 | Old Supply Chain Window | **Keep both** for now (dashboard diagram may stay unused in 2D map flow). |
| Q6 | Resource icons on curves | **Defer** — no midpoint resource icons in P0/P1 (see note below). |
| Q7 | Reduced motion | **Defer** — ship moving dots; add accessibility later if needed. |
| Q8 | Chain-complete celebration | **Yes** — FeedbackBus one-shot; ack stored in `run_stats` so it doesn’t repeat every open. |

### Notes on Q6 / Q7 (plain language)

**Q6 — Resource icons:** Spec optional: tiny Grain / Feed / Eggs icons floating at the middle of each curve. Nice for reading “what flows,” but cluttered and needs art. **Decision: skip for now**; only show that info in the route tooltip text (e.g. `Resource: Grain`).

**Q7 — Reduced motion:** Some players get motion-sick from looping dots. Spec said: if “reduce motion” is on, use a static arrow instead. **Decision: skip for now**; always use the traveling dot. We can add an option later.

---

## P0 — Foundation (ship a readable overlay)

### Map UI toggle
- [ ] Add **Supply Chain** toggle button on farm map HUD (chain-link icon + clear on/off state)
- [ ] States: Off = normal map · On = routes visible; label/tooltip “Supply Chain” / “Hide Supply Chain”
- [ ] Wire into `iso_farm_map` + click-through HUD rules (`iso_farm_map_ui`, `_pointer_over_ui`)

### Graph service (logic, not rendering)
- [ ] `SupplyChainGraphService` — build graph from `Content.connections` + run portfolio / parcel assignments
- [ ] Node identity: map `businessId` / `templateId` → parcel / world anchor
- [ ] Edge model: `{ id, sourceId, targetId, resourceType/flow, playerControlled }`
- [ ] `get_connection_visual_state(connection) → "external" | "owned" | "complete"` (single ownership API; no ownership math in draw code)
- [ ] Unit tests: ownership classification + no-crash empty district

### Path resolver
- [ ] `SupplyChainPathResolver.build_supply_chain_paths(selectedId, graph) → SupplyChainPath[]`
- [ ] Upstream + downstream, multi-level, branches as separate paths
- [ ] Cycle / revisit guard (no infinite loops)
- [ ] `isComplete` per path = every edge on path is player-controlled
- [ ] Unit tests: middle node, leaf, branch, cycle fixture

### Visualization controller / state
- [ ] `SupplyChainViewState` (enabled, selectedBusinessId, paths, activePathIndex, autoCycle, timers)
- [ ] Actions: enable / disable / select / next / prev / pause / resume
- [ ] On enable: auto-select by priority rules (spec §16)
- [ ] On disable: clear routes, stop timers, restore normal click behavior
- [ ] Do not persist screen-space geometry in `RunState`

### Route layer (map draw)
- [ ] `SupplyChainRouteLayer` Node2D above buildings, below HUD/modals
- [ ] Draw **one active path** as elevated Bezier dotted arcs
- [ ] Colors: white external · blue owned · green complete path
- [ ] Shared animation clock for direction dots (1.5–2.5s, length-scaled)
- [ ] Recalc graph/paths only on selection / ownership / district change; update screen points with camera each frame

### Selection while mode active
- [ ] Click building → select for visualization (replace prior chain)
- [ ] No-connection chip: “No supply chain connections” (auto-dismiss)
- [ ] Selected business distinct highlight (outline / ring) without hiding art
- [ ] Implement agreed click model (Q2)

### Acceptance (P0)
- [ ] Toggle on/off works
- [ ] Auto-selects a connected business when possible
- [ ] Full path shown (not only neighbors)
- [ ] Curved dotted lines + direction motion
- [ ] White / blue / green states correct per ownership API
- [ ] Timers stop when disabled
- [ ] Does not steal modal / Advance Turn / top-bar input

---

## P1 — Branches, UX polish, updates

### Path cycling & controls
- [ ] Auto-cycle branches every **3s** with crossfade
- [ ] Compact “Path N of M” indicator
- [ ] Optional ‹ / Pause / › controls
- [ ] Pause on: hover controls, parcel panel open, modal open, game pause
- [ ] Resume only when viz still active and unblocked

### Completion juice
- [ ] Green path + subtle building treatment when path complete
- [ ] “Complete Supply Chain” chip / label
- [ ] One-shot celebration when a path **becomes** complete in-run (ack in `run_stats`)

### Live updates
- [ ] Refresh on buy/sell, unlock, agreement/synergy change, district focus change
- [ ] Sold/removed selection → re-pick or clear gracefully
- [ ] Ownership flip while visible → recolor immediately

### Anchors & readability
- [ ] Per-building route anchors (door / top fallback); incoming vs outgoing if useful
- [ ] Arc height scales with distance; min height; offset overlapping pairs
- [ ] Route hover tooltip (A → B, resource, status) without blocking clicks

### Optional / config
- [ ] `showSupplyResourceIcons` flag (default off until icons exist)
- [ ] Reduced-motion: static arrow / slow pulse instead of traveling dot

### Acceptance (P1)
- [ ] Branched paths alternate cleanly
- [ ] Path indicator readable
- [ ] Camera pan/zoom keeps endpoints glued
- [ ] Circular data never freezes
- [ ] Style matches warm isometric farm UI (no neon hologram look)

---

## P2 — Hardening & content

- [ ] Performance pass: cache Bezier samples; one clock; dispose on disable/district change
- [ ] Edge cases checklist from spec §21 exercised in GUT / playtest
- [ ] Overview vs district dimming interaction (only draw relevant district paths if Q3 says so)
- [ ] Accessibility pass (tooltips, reduced motion, contrast on bright terrain + drop shadow)
- [ ] Docs: short player-facing note + update `8.0` / architecture pointer if needed
- [ ] Decide fate of old Supply Chain Window (Q5)

---

## Suggested file layout (Godot)

```
godot/core/community/…                    # (no — keep social separate)
godot/core/systems/supply_chain_graph_service.gd
godot/core/systems/supply_chain_path_resolver.gd
godot/core/systems/supply_chain_ownership.gd   # get_connection_visual_state
godot/scenes/farm_map/supply_chain_route_layer.gd
godot/scenes/farm_map/supply_chain_view_controller.gd
godot/ui/screens/… path controls / chips as needed
godot/tests/test_supply_chain_graph.gd
godot/tests/test_supply_chain_paths.gd
```

Toggle lives on `iso_farm_map` HUD next to other chrome.

---

## Suggested implementation order

```
1. GraphService + ownership API + GUT fixtures
2. PathResolver + branch/cycle tests
3. RouteLayer Bezier dotted draw + shared clock
4. Controller + HUD toggle + auto-select
5. Map click selection in viz mode
6. Colors + direction dots
7. Path cycling UI + pause rules
8. Completion juice + live EventBus hooks
9. Anchors, tooltips, polish, reduced-motion
```

---

## Acceptance notes (from spec)

- Viz overlays the map; does not replace building art.
- Ownership logic stays centralized; renderer only consumes visual state.
- Only active path (or necessary shared edges) is drawn.
- No permanent clutter when mode is off.
- Match farm pixel-art language: chunky, warm, clear contrast.
